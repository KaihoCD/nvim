# TODO

- `heirline` 的tab列表的顺序, 旧的标签应该保存原本的顺序, 现在是旧的标签的序号会随着标签的添加而递增

- `heirline` 需要重新实现一版, 删除buffer仅从当前tab中删除元素, 当buffer不存在任何标签中的时候才会执行删除buffer

- [!] 当打开一个termnial窗口的时候, 然后输入`exit`退出终端
  然后打开终端的时候, 不会重载这个终端, 而是打开一个空的`buffer`
  并且这个buffer的背景色也有问题，它不是正常buffer的背景色, 似乎是
  浮动窗口的背景色

- [!] 想markdown的格式化工具, 就应该有一个默认的配置, 而不是没有这个配置就报错

- 图标的配置, 不应该是通过简单加一个后缀空格来解决的
  而是要通过判断图标的宽度, 宽度不足2的就补一个空格,应该是这么做的

- [?] `prettierd` 这样的格式化插件, 可能会格式化的时报错, 这时候可以选择自动重试, 如果重试失败直接关闭
  重试的次数再商榷
